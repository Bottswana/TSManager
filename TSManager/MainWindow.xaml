<Window x:Class="TSManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TSManager"
        mc:Ignorable="d"
        Title="Session Task Manager" Height="500" Width="525" Icon="Resources/taskmgr.ico" Closing="_Window_Closing" Loaded="_Window_Loaded">
    <Window.Resources>

        <Style TargetType="Button">
            <Setter Property="BorderBrush" Value="Silver"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Width" Value="70"/>
        </Style>

    </Window.Resources>
    <Grid>
        <TabControl x:Name="WindowTab" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SelectionChanged="_WindowTab_SelectionChanged" Margin="0,2,0,0"></TabControl>
    </Grid>
</Window>
